# contact.csv
This Python program is a **simple contact book** using the `csv` module to store data.   It allows you to **add** new contacts and **view** saved contacts in a readable format.   The contacts are saved in a file called `contact.csv`, and the program runs through a **menu system**.
